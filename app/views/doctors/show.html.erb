<h1><%= @doctor.name %> Appoinments</h1>
<ul>
  <% @doctor.appointments.each do |appt| %>
  <li><%= link_to appt.patient.name, patient_path(appt.doctor)%> on <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
  <% end %>

</ul>
