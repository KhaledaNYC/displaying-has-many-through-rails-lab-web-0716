<h1><%= @patient.name %>'s Appointments</h1>


<ul>
  <% @patient.appointments.each do |appt| %>
  <li><%= link_to appt.doctor.name, doctor_path(appt.doctor)%> on <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
  <% end %>
</ul>
